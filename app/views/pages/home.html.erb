<body>
  <div id="banner">
    <div class="banner-content">
      <h1>Meet a Talent.</h1>
      <h2>Passionate people sharing</h2>
      <h2>their experience around a drink.</h2>
      <br>
      <form action="<%= talents_path %>" class="flex-form navbar-left" role="search" style="padding-left: 0px">
        <input type="text" name="query" class="form-control" id="home-search" placeholder="Search">
        <select type="text" name="category" id="home-categories" class="form-control" placeholder="Select a category">
          <% @categories.each do |category| %>
            <option value="<%= category.id %>"><%= category.name %></option>
          <% end %>
        </select>
        <button type="submit" class="btn btn-primary">Search</button>
      </form>
    </div>
  </div>
</body>
